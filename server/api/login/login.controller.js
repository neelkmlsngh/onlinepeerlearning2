const UserModel = require('./login.entity');
const logger = require('../../services/app.logger');
const appConstant = require('../../config').app;

//Save new student's details
const registerNewUser = function() {
	
};


module.exports = {
    registerNewUser: registerNewUser,
};